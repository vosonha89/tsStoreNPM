language: node_js
  
before_script:
  - cd tsStore

env:
  - workerCount=3 timeout=600000

matrix:
  fast_finish true

branches:
  only:
  - master

install:
  - npm install

cache:
  directories:
    - node_modules

git:
  depth 1
